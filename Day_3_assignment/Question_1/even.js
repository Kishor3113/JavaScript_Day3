console.log("Check Number");

let num=+prompt("enter number",0);

if(num%2==0)
{
    console.log(`The number entered is ${num} and number even`);
}
else
{
    console.log(`The number entered is ${num}  Odd`);
}